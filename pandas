#https://www.kaggle.com/code/residentmario/indexing-selecting-assigning?scriptVersionId=87319685&cellId=28

# and = &
# or = |
# where 문 사용법 in pandas 
# reviews.loc[(reviews.country == 'Italy') | (reviews.points >= 90)]
# isin[]
# reviews.loc[reviews.country.isin(['Italy', 'France'])]

# .isna()
# .notnull()
# reviews.loc[reviews.price.notnull()]
